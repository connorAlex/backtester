
interface IUser {
    name: string;
    email: string;
    password: string;
    createdDate: Date;
}

interface rawUserData {
    name: string;
    email: string;
    password: string;
    createdDate: Date;
}